# Medición diagnóstica inicial: criterio incumplido

El RSS headless creció y el pico con trabajador superó la referencia. Se conserva íntegra esta medición previa a reutilizar sentencias SQLite; no es una validación aprobada.

El primer baseline coincidió parcialmente con la reconstrucción de fútbol. La cuenta operativa anterior siguió en el mismo host (incluido informe horario); CPU y latencia tuvieron contención. Los cuatro hashes de entrada coinciden, pero esa interferencia impide atribuir diferencias temporales solo al código. El protocolo inicial cargaba el módulo de informe también en el padre headless; la medición final carga los módulos de producción correspondientes.
